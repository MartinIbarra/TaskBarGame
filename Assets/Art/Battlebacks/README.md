Battleback source art lives here.

Runtime copies used by `Resources.Load` live in `Assets/Resources/Battlebacks`.
Keep this folder as the editable/source side, and copy only approved runtime PNGs
to Resources when they are ready to be used in combat.
