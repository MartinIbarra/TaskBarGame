# Maps

`Source/map1.psd` is the editable source for the first expedition map.

Unity runtime uses PNG files from `Assets/Resources/Maps`:

- `map1.png`: base map image.

Challenge uses the base map and highlights every node in white. Safety and Loot
currently use the editable generated `Routes` and `Nodes` hierarchy until new
artist-authored path overlays are added.
